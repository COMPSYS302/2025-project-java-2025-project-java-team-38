<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/hanger"
        android:label="@string/app_name"
        android:roundIcon="@drawable/rounded_title"
        android:supportsRtl="true"
        android:theme="@style/Theme.Closet"
        >
        <activity
            android:name=".DetailsActivity"
            android:theme="@style/Theme.Closet"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <!--add here for when a page is finished being built -->
        <!--<activity android:name=".ShirtsActivity" />
        <activity android:name=".JeansActivity" />
        <activity android:name=".AccessoriesActivity" />
        -->
    </application>

</manifest>